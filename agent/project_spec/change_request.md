langchain, langgraph 를 사용해보려 합니다.

라이브러리를 이용하되, 간단한 기능으로 구현해주세요.

일단 rag 서치와 같은 코드 없이, 
일반 질문을 하면, llm 이 순수하게 응답하는 코드 레벨이면 됩니다.


그런 기능을 구현하되, 소스는 단순히 def 함수들로 정의하지 말고,
추후 프로덕트 운영레벨로의 확장성을 고려해서
모듈화든 싱글톤이든 객체지향이든, 어떤 설계가 적합할지 모르겠지만 적절한 형태를 고려해서 작성해주세요.  

llm 연결에 필요한 정보는 환경변수 파일 .env 파일을 가져다 쓸 수 있도록 해주세요.